import styled from 'styled-components'
import banner from '../../assets/header-logo.svg'

export const HeaderContainer = styled.header`
  background-image: url(${banner});
  background-size: cover;
  display: flex;
  justify-content: center;
  align-items: center;
  height: 18.5rem;
`
